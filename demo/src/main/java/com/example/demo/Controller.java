package com.example.demo;


import javafx.fxml.FXML;
import javafx.fxml.Initializable;
import javafx.scene.canvas.Canvas;
import javafx.scene.canvas.GraphicsContext;
import javafx.scene.control.Button;

import javafx.scene.control.TextField;
import javafx.scene.image.PixelWriter;
import javafx.scene.input.KeyCode;


import javafx.scene.paint.Color;


import java.net.URL;
import java.util.ResourceBundle;

public class Controller  {


    @FXML
    void drawEllipce(final GraphicsContext graphicsContext, int X0, int Y0, // центр эллипса (x,y)
              double a, // большая полуось
              double b, // малая полуось
              Color startColor, // начальный цвет для интерполяции
              Color endColor) {// конечный цвет для интерполяции

//        final PixelWriter pixelWriter = graphicsContext.getPixelWriter();

        }
    }
